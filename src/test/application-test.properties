# Esto se activa automáticamente cuando se ejecuta @SpringBootTest
# Usa H2 en memoria para que las pruebas pasen sin depender de MySQL
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Mantenemos las configuraciones de DDL y SQL solo si es necesario para las pruebas
spring.jpa.hibernate.ddl-auto=create-drop